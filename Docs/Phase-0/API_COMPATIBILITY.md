# API Compatibility

Rules:
- `/api/v1` is stable.
- Breaking changes require explicit versioning.
- Deprecation and sunset policies must be documented.
- Contract tests should guard public API behavior.
- Idempotency semantics must remain consistent.

